# Seed Sweep 20260701 Interpretation

## Run status

The `20260701` seed-sweep run completed the expected eight-condition, 64-replicate internal TDA artifact scale:

- Streams: 512
- Embeddings: 1,024
- TDA feature rows: 2,048
- Internal randomness rows: 512
- Distance-to-baseline rows: 2,048
- Backend: `ripser`
- Fallback detected: `False`

This is manuscript-grade for internal TDA claims, subject to the normal claim-control boundary.

## External randomness status

The evidence-register excerpt for this run showed the external randomness table and status report as missing rather than unavailable or populated:

- `data/processed/external_randomness_tests.csv`: missing
- `results/logs/external_randomness_status.md`: missing
- External runner status: missing

This does not affect the internal TDA evidence claim, but it should be normalized before final evidence registration by running the failure-safe external randomness status step and rebuilding the evidence register.

## Effect-size interpretation

The top-40 effect table supports the intended seed-sweep pass condition. The strongest rows are dominated by deliberately structured controls:

- `biased_byte_weak` shows maximum separation across sliding-window H1 distance-to-baseline, byte-pair H1 distance-to-baseline, sliding-window H1 persistence entropy, sliding-window H0 persistence entropy, sliding-window H0 distance-to-baseline, and byte-pair H0 distance-to-baseline.
- `periodic_byte_weak` shows maximum or near-maximum separation across byte-pair H0/H1 distance-to-baseline, byte-pair H0/H1 persistence entropy, sliding-window H0 distance-to-baseline, sliding-window H1 distance-to-baseline, and sliding-window H0 persistence entropy.
- `lcg_weak` remains a strong positive-control signal under byte-pair H0 persistence entropy and byte-pair H0 distance-to-baseline.

AES-CTR, OS CSPRNG, and xorshift32 remain much smaller in the reviewed top-40 rows and do not show comparable broad top-ranked separation.

## Supported incremental claim

The seed-sweep 20260701 run reinforces the existing weak-control sensitivity pattern under an additional deterministic master seed. The supported interpretation is that deliberately structured weak controls remain the strongest separators from the SHA-256 seeded baseline under the registered internal TDA summaries.

This is not cryptanalysis, a cipher-vulnerability claim, or security certification.

## Required next actions

1. Run the external randomness status step with missing-Dieharder allowed.
2. Rebuild `docs/evidence_register_seed_sweep_20260701.md`.
3. Confirm the external randomness section records `UNAVAILABLE` or populated rows rather than `missing`.
4. Commit the evidence register.
5. Run the `20260702` seed-sweep config only after the `20260701` evidence register is committed.
