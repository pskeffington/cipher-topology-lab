# Seed Sweep 20260702 Interpretation

## Run status

The `20260702` seed-sweep effect table supports the planned second-seed pass criterion. The top-ranked absolute Cliff's delta rows are again dominated by deliberately structured weak controls rather than AES-CTR, OS CSPRNG, or xorshift32.

## Effect-size interpretation

The strongest rows reproduce the same broad pattern observed in earlier weak-control and seed-robustness runs:

- `biased_byte_weak` reaches maximum separation in sliding-window H1 distance-to-baseline, byte-pair H1 distance-to-baseline, sliding-window H1 persistence entropy, sliding-window H0 distance-to-baseline, sliding-window H0 persistence entropy, byte-pair H1 persistence entropy, byte-pair H0 distance-to-baseline, and byte-pair H0 persistence entropy.
- `periodic_byte_weak` reaches maximum or near-maximum separation in byte-pair H1 persistence entropy, byte-pair H0 distance-to-baseline, byte-pair H1 distance-to-baseline, sliding-window H0 distance-to-baseline, byte-pair H0 persistence entropy, sliding-window H0 persistence entropy, sliding-window H1 distance-to-baseline, and sliding-window H1 persistence entropy.
- `lcg_weak` remains a strong positive-control signal in byte-pair H0 persistence entropy and byte-pair H0 distance-to-baseline, with additional smaller effects in byte-pair H1 persistence entropy and other summaries.

AES-CTR, OS CSPRNG, and xorshift32 appear only in smaller effect rows in the reviewed top-40 effect table. They do not show the broad, repeated, top-ranked separation pattern seen for the deliberately biased and periodic controls.

## Supported incremental claim

The `20260702` seed-sweep result reinforces the claim that the registered internal TDA workflow detects deliberately structured weak controls across multiple deterministic master seeds. The result strengthens seed-stability footing for the diagnostic thesis.

The claim remains bounded: this is diagnostic and reproducibility evidence, not cryptanalysis, a cipher-vulnerability finding, or security certification.

## Required next actions

1. Ensure the external randomness status is normalized to `UNAVAILABLE` or populated, not silently missing.
2. Build and commit `docs/evidence_register_seed_sweep_20260702.md`.
3. Update manuscript and portfolio evidence maps after both seed-sweep evidence registers are present.
4. Proceed to the 128-replicate scale check only after the `20260702` evidence register is committed.
