# Lab Note — 2026-06-25

## Workstream

Cipher topology laboratory workstream for topological diagnostics of symmetric-cipher output randomness.

## Current laboratory focus

The near-term lab focus is to stabilize the evaluation frame before adding new cipher conditions or stronger public claims.

Priority work items:

1. Freeze the null and control design.
2. Document the contribution boundary.
3. Add a notebook structure that supports exploration without becoming the evidence source of record.
4. Reconcile the evidence register with README and publication-plan claims before strengthening manuscript language.
5. Add an execution-control plan from smoke workflow to 64-replicate evidence package.

## Repository facts checked

The repository README frames the project as a reproducible research workspace evaluating whether persistent homology can serve as an auxiliary diagnostic for symmetric-cipher output randomness. It explicitly states that the project does not claim to break AES, Ascon, DES, or any standardized cipher.

The current protocol identifies the manuscript evidence configuration as `configs/experiment_64rep.json`, using 64 replicates, six conditions, two embeddings, and H0/H1 persistent-homology summaries with a manuscript-grade `ripser` backend.

The protocol also identifies the configured conditions as:

- `aes128_ctr_xor_deterministic_plaintext`
- `aes128_ctr_keystream_zero_plaintext`
- `sha256_seeded_baseline`
- `os_csprng`
- `lcg_weak`
- `xorshift32_weak`

## Evidence-state caution

A documentation mismatch is currently visible:

- README and publication-plan language describe the 64-replicate evidence package and stable internal findings.
- `docs/evidence_register.md` reports a failed validation state with missing required artifacts, including `data/interim/embedding_manifest.csv`, missing TDA feature tables, and no detected TDA backend.

Until the evidence register is regenerated or corrected, the stricter claim-control stance should govern public language. The project can describe its pipeline, design, and intended evidence structure, but should avoid overstating completed manuscript-grade evidence unless the current register confirms the artifacts.

## Files added in first lab pass

- `docs/null_control_design.md`
- `docs/contribution_boundary.md`
- `notebooks/README.md`

## Files added in second lab pass

- `docs/evidence_reconciliation_checklist.md`
- `docs/run_plan_smoke_to_64rep.md`

## Second-pass purpose

The second pass converts the earlier boundary documentation into an executable evidence-control pathway. The key aim is to avoid a situation where README, publication-plan, or manuscript language describes a completed 64-replicate evidence package while the current evidence register still reports missing artifacts or failed validation.

## Next technical tasks

1. Run or rerun the smoke workflow and confirm that generated artifacts populate expected locations.
2. Run smoke artifact coherence and artifact consistency checks.
3. Regenerate the evidence register from the smoke workflow.
4. Run the 64-replicate segmented workflow only after smoke-stage artifact checks pass.
5. Run 64-replicate coherence, consistency, effect-size, and evidence-register stages.
6. Compare regenerated evidence register with README and publication plan.
7. Promote only registered results into manuscript language.

## Claim boundary for next public update

Use:

> Reproducible computational-diagnostics workspace evaluating whether persistent-homology summaries can distinguish structured or weak generator controls from deterministic cryptographic baselines under controlled cipher-output experiments.

Avoid:

> This breaks, detects insecure, proves, certifies, or validates standardized cipher security.

## Stop condition

If the regenerated evidence register still reports missing TDA artifacts, failed coherence, fallback backend evidence, or unavailable external randomness rows, public language should remain at design/workflow level rather than completed-evidence level.
