# Daily documentation update — 2026-05-26

## Scope

This update records the current evidence state for the cipher-topology-lab manuscript workflow and clarifies how the generated TDA and randomness artifacts should be interpreted.

## Current evidence snapshot

The current manuscript-scale run is organized around `configs/experiment_64rep.json`. The run uses 64 replicates, two embedding families, H0/H1 persistent-homology summaries, and the `ripser` backend for the primary internal analysis.

Current generated artifact families include:

- `tda_features.csv`
- `tda_feature_summary.csv`
- `tda_backend_summary.csv`
- `tda_distance_to_os_csprng.csv`
- `tda_distance_effects.csv`
- `tda_persistence_entropy_effects.csv`
- `tda_effects_combined.csv`
- `randomness_tests_internal.csv`
- `external_randomness_tests.csv`

The main stable internal signal remains weak-control separation for the LCG condition under byte-pair H0 summaries relative to the SHA-256 seeded deterministic baseline. AES-CTR, OS CSPRNG, and xorshift32 do not show comparable separation under the current feature set.

## Figure set now being tracked

The current visual evidence set includes persistence-entropy and distance-to-OS-CSPRNG figures for these backend/embedding/dimension combinations:

- `ripser` / `byte_pair_2d` / H0 and H1
- `ripser` / `sliding_window_8d` / H0 and H1
- `gudhi_cubical` / `cubical_image_2d` / H0 and H1

These figures should be treated as diagnostic summaries, not cryptanalytic claims. They support the manuscript framing that persistent homology may serve as an auxiliary randomness diagnostic when interpreted alongside conventional statistical tests.

## Documentation guidance

Use the README for the high-level project state and terminal commands. Use this daily update as the dated evidence note for May 26, 2026. The evidence register should remain the canonical manuscript-facing index of generated outputs, validation status, and interpretation constraints.

## Next documentation targets

- Reconcile the evidence register against the current CSV and figure inventory.
- Add a short figure-index table mapping each PNG to backend, embedding, homology dimension, and diagnostic family.
- Keep external randomness status explicit when Dieharder is unavailable, rather than treating unavailable infrastructure as completed evidence.
- Preserve the claim boundary: diagnostic/reproducibility contribution, not cipher breaking.
